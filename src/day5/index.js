const processInstructions = function processInstructions(instructions) {

  return 0;
};

const taskOne = function taskOne(instructionString) {
  const instructions = instructionString.split('\n');
  const steps = processInstructions(instructions);
  return steps;
};

const taskTwo = function taskTwo() {

};

module.exports = {
  taskOne,
  taskTwo,
};
